{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.ts|*.tsx) npx eslint \"$f\" 2>&1 | head -c 4000 ;; esac; }; true",
            "timeout": 30,
            "statusMessage": "Lint del archivo tocado (C29/Fase 0)"
          }
        ]
      }
    ]
  }
}
